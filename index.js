function theBeatlesPlay(musicians, instruments) {
  var output = [];
  for (i = 0; i < musicians.length; i++) {
    var sentence = musician[i] + ' plays the ' + instruments[i];
    output.push(sentence);
  }
}
